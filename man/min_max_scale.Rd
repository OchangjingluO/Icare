% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-2.R
\name{min_max_scale}
\alias{min_max_scale}
\title{Min-Max Scaling of Input Data}
\usage{
min_max_scale(x)
}
\arguments{
\item{x}{A data frame or matrix to be scaled. Each column will be scaled independently.}
}
\value{
A list containing:
\itemize{
\item \code{scaled_data}: A data frame or matrix with Min-Max scaled values.
\item \code{normalize_method}: A string indicating the normalization method used (Min-Max scaling).
\item \code{normalize_info}: A list containing the \code{min_vals} and \code{max_vals} for each column.
}
}
\description{
This function performs Min-Max scaling on the input data. It normalizes each
column by subtracting the minimum value and dividing by the range (max - min)
for that column. It checks if any columns have constant values, as Min-Max scaling
cannot be performed on columns where the minimum and maximum values are the same.
}
\examples{
# Example of using min_max_scale
x <- data.frame(A = c(1, 2, 3), B = c(4, 5, 6))
result <- min_max_scale(x)
result$scaled_data  # The scaled values
result$normalize_info  # The min and max values used for scaling

}
