% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-2.R
\name{scale_data}
\alias{scale_data}
\title{Scale Data (Standardization)}
\usage{
scale_data(x)
}
\arguments{
\item{x}{A data frame or matrix to be standardized. Each column will be scaled independently.}
}
\value{
A list containing:
\itemize{
\item \code{scaled_data}: A data frame or matrix with standardized values (scaled by the standard deviation of each column).
\item \code{normalize_method}: A string indicating the normalization method used (scale_data).
\item \code{normalize_info}: A list containing the scaling values (standard deviations for each column).
}
}
\description{
This function standardizes the input data by dividing each column by its standard deviation.
Columns with zero standard deviation will be handled by replacing their standard deviation with 1,
ensuring no division by zero errors occur. The data is transformed such that each column has unit variance.
}
\examples{
# Example of using scale_data
data <- data.frame(A = c(1, 2, 3), B = c(4, 5, 6))
result <- scale_data(data)
result$scaled_data  # The scaled values
result$normalize_method  # Method used for normalization

}
