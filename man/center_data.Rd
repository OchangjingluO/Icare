% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-2.R
\name{center_data}
\alias{center_data}
\title{Center Data (Zero Mean)}
\usage{
center_data(data)
}
\arguments{
\item{data}{A data frame or matrix to be centered. Each column will be centered independently.}
}
\value{
A list containing:
\itemize{
\item \code{scaled_data}: A data frame or matrix with centered values (mean subtracted).
\item \code{normalize_method}: A string indicating the normalization method used (center_data).
\item \code{normalize_info}: A list with additional information (empty in this case).
}
}
\description{
This function centers the input data by subtracting the mean from each column.
The data is transformed such that each column has a mean of zero. The scale argument
is set to \code{FALSE}, meaning no scaling (division by standard deviation) is applied,
only centering (shifting values to zero mean).
}
\examples{
# Example of using center_data
data <- data.frame(A = c(1, 2, 3), B = c(4, 5, 6))
result <- center_data(data)
result$scaled_data  # The centered values
result$normalize_method  # Method used for normalization

}
